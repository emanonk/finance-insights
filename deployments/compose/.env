# Copy to .env and adjust as needed.
# `docker compose` in this directory will load this file automatically.

# PostgreSQL
POSTGRES_USER=finance
POSTGRES_PASSWORD=finance
POSTGRES_DB=finance_insights
POSTGRES_PORT=5432

# API (Go backend)
API_PORT=8080

# Web (frontend served by nginx)
WEB_PORT=5173
